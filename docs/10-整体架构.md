# 整体架构

> 🏗️ 6 层架构全景 | 理解 Claude Code 的系统设计

---

## 🎯 学习目标

完成本节后，你将理解：
- ✅ 6 层架构的职责划分
- ✅ 核心组件的交互关系
- ✅ 数据流的走向
- ✅ 关键设计决策

---

## 1. 6 层架构全景

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户交互层                                │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐  │
│  │ CLI 命令行参数 │  │ 交互式对话输入 │  │ Slash Commands (/config) │  │
│  └────────┬────┘  └────────┬────┘  └─────────────┬───────────┘  │
└───────────┼────────────────┼─────────────────────┼──────────────┘
            │                │                     │
            └────────────────┼─────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│                      表现层 (Presentation Layer)                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  Ink (React for Terminal) - 146 个组件 / 50 个核心渲染模块   ││
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────────────┐   ││
│  │  │ App.tsx     │ │ REPL.tsx    │ │ PromptInput         │   ││
│  │  └─────────────┘ └─────────────┘ └─────────────────────┘   ││
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────────────┐   ││
│  │  │ Dialogs     │ │ Progress    │ │ ToolResultViews     │   ││
│  │  └─────────────┘ └─────────────┘ └─────────────────────┘   ││
│  └─────────────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────────────┤
│                      状态管理层 (State Layer)                    │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  AppStateStore.ts (22KB) - 统一状态管理                      ││
│  │  store.ts (836B) - 发布订阅原语                             ││
│  │  87 个 React Hooks - 业务逻辑封装                            ││
│  └─────────────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────────────┤
│                      核心引擎层 (Core Engine)                    │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │ main.tsx        │  │ query.ts        │  │ QueryEngine.ts  │  │
│  │ (804KB 入口)    │  │ (69KB 编排)     │  │ (47KB 引擎)     │  │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘  │
│           │                    │                    │            │
│           └────────────────────┼────────────────────┘            │
│                                │                                 │
│                    ┌───────────┴───────────┐                     │
│                    │                       │                     │
│           ┌────────▼────────┐     ┌────────▼────────┐           │
│           │ Tool.ts (30KB)  │     │ tools.ts (17KB) │           │
│           │ (工具抽象)      │     │ (工具注册表)    │           │
│           └─────────────────┘     └─────────────────┘           │
├─────────────────────────────────────────────────────────────────┤
│                        工具层 (Tool Layer)                       │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  45 个工具实现目录                                           ││
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐   ││
│  │  │ BashTool │ │FileEdit  │ │FileRead  │ │ Glob/Grep    │   ││
│  │  │ (160KB)  │ │ (8 files)│ │ (7 files)│ │ (5 files ea) │   ││
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────────┘   ││
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐   ││
│  │  │ AgentTool│ │ SkillTool│ │ MCPTool  │ │ WebFetch     │   ││
│  │  │(17 files)│ │ (7 files)│ │ (6 files)│ │ (7 files)    │   ││
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────────┘   ││
│  └─────────────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────────────┤
│                      服务层 (Service Layer)                      │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  38 个服务模块                                               ││
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐   ││
│  │  │ mcp/     │ │ lsp/     │ │ api/     │ │ oauth/       │   ││
│  │  │ (25 文件) │ │ (7 文件)  │ │ (22 文件) │ │ (7 文件)      │   ││
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────────┘   ││
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐   ││
│  │  │ analytics│ │ compact/ │ │ plugins/ │ │ voice/       │   ││
│  │  │ (11 文件) │ │(13 文件)  │ │ (5 文件)  │ │ (3 文件)      │   ││
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────────┘   ││
│  └─────────────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────────────┤
│                      扩展层 (Extension Layer)                    │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │ skills/         │  │ plugins/        │  │ bridge/         │  │
│  │ (6 文件 + 19 技能)│  │ (5 文件)        │  │ (33 文件)       │  │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│                    基础设施层 (Infrastructure)                   │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐  │
│  │ utils/      │  │ types/      │  │ constants/              │  │
│  │ (331 文件)   │  │ (10 文件)    │  │ schemas/ migrations/    │  │
│  └─────────────┘  └─────────────┘  │ (23+3+13 文件)          │  │
│  ┌─────────────┐  ┌─────────────┐  └─────────────────────────┘  │
│  │ ink/        │  │ components/ │                               │
│  │ (50 文件)    │  │ (146 文件)   │                               │
│  └─────────────┘  └─────────────┘                               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. 各层职责详解

### 2.1 用户交互层

**职责**: 接收用户输入，提供多种交互方式。

**交互方式**:

| 方式 | 示例 | 说明 |
|------|------|------|
| **CLI 参数** | `claude "fix the bug"` | 命令行直接传入 |
| **交互式对话** | REPL 模式逐轮对话 | 终端交互界面 |
| **Slash Commands** | `/config`, `/review` | 内置命令 |

**核心文件**:
- `main.tsx` - CLI 入口，解析参数
- `commands.ts` - 命令注册表
- `REPL.tsx` - 交互式界面

---

### 2.2 表现层

**职责**: 在终端中渲染 UI，处理用户输入。

**技术栈**: Ink (React for Terminal)

**核心组件**:

```
components/
├── App.tsx                      # 主应用组件
├── REPL.tsx                     # REPL 界面
├── PromptInput.tsx              # 输入框
├── Message.tsx                  # 消息组件
├── ToolResultViews/             # 工具结果视图
├── PermissionDialog.tsx         # 权限对话框
├── Spinner.tsx                  # 加载动画
└── ... (共 146 个组件)
```

**Ink 渲染模块**:

```
ink/
├── render-to-screen.ts          # 屏幕渲染
├── layout/engine.ts             # 布局引擎
├── layout/yoga.ts               # Yoga 布局
├── colorize.ts                  # 语法高亮
└── ... (共 50 个模块)
```

---

### 2.3 状态管理层

**职责**: 管理应用全局状态，提供状态更新和订阅机制。

**核心组件**:

```typescript
// store.ts - 最小状态管理原语 (836B)
type Listener = () => void
type OnChange<T> = (args: { newState: T; oldState: T }) => void

export function createStore<T>(initialState: T, onChange?: OnChange<T>): Store<T> {
  let state = initialState
  const listeners = new Set<Listener>()

  return {
    getState: () => state,
    setState: (updater: (prev: T) => T) => {
      const next = updater(state)
      if (Object.is(next, state)) return
      state = next
      onChange?.({ newState: next, oldState: state })
      listeners.forEach(fn => fn())
    },
    subscribe: (listener: Listener) => {
      listeners.add(listener)
      return () => listeners.delete(listener)
    },
  }
}
```

**状态定义**:

```typescript
// AppStateStore.ts - 统一状态定义 (22KB)
export type AppState = DeepImmutable<{
  // 配置
  settings: SettingsJson
  verbose: boolean
  mainLoopModel: ModelSetting
  
  // UI
  statusLineText: string | undefined
  expandedView: 'none' | 'tasks' | 'teammates'
  
  // 权限
  toolPermissionContext: ToolPermissionContext
  
  // MCP
  mcp: {
    clients: MCPServerConnection[]
    tools: Tool[]
    commands: Command[]
  }
  
  // 任务
  tasks: { [taskId: string]: TaskState }
  
  // ... 更多状态
}>
```

**Hooks 系统**:

```
hooks/
├── useCanUseTool.tsx            # 工具权限检查
├── useArrowKeyHistory.tsx       # 箭头键历史
├── useCommandKeybindings.tsx    # 命令快捷键
├── useSettings.ts               # 设置管理
└── ... (共 87 个 Hooks)
```

---

### 2.4 核心引擎层

**职责**: 处理用户请求，调度模型调用，执行工具。

**核心文件**:

| 文件 | 大小 | 职责 |
|------|------|------|
| `main.tsx` | 804KB | CLI 入口、初始化、渲染启动 |
| `query.ts` | 69KB | 查询编排、API 调用、流式处理 |
| `QueryEngine.ts` | 47KB | 会话引擎、消息提交、工具调度 |
| `Tool.ts` | 30KB | 工具抽象接口、类型定义 |
| `tools.ts` | 17KB | 工具注册表、条件加载 |

**查询循环流程**:

```
┌─────────────────────────────────┐
│ QueryEngine.submitMessage()     │
│ - 包装 canUseTool (追踪拒绝)     │
│ - 构建系统提示                   │
│ - 处理用户输入                   │
└───────────────┬─────────────────┘
                │
                ↓
┌─────────────────────────────────┐
│ query() 生成器                   │
│ - 应用压缩 (Snip/Microcompact)  │
│ - 调用 Claude API (流式)        │
│ - 检测工具调用                   │
└───────────────┬─────────────────┘
                │
          ┌─────┴─────┐
          │           │
    [流式响应]   [tool_use]
          │           │
          │           ↓
          │   ┌─────────────────┐
          │   │ runTools()      │
          │   │ - 权限检查       │
          │   │ - 执行工具       │
          │   │ - 返回结果       │
          │   └────────┬────────┘
          │            │
          └────────────┴────────┐
                               │
                               ↓
                    ┌─────────────────────────┐
                    │ 结果回灌到 query 循环     │
                    │ - 构建 tool_result      │
                    │ - 继续循环或完成         │
                    └─────────────────────────┘
```

---

### 2.5 工具层

**职责**: 提供 AI 可调用的功能单元。

**工具分类**:

```
tools/
├── 核心工具 (6 个)
│   ├── BashTool/           # Shell 执行 (160KB)
│   ├── FileReadTool/       # 文件读取
│   ├── FileWriteTool/      # 文件写入
│   ├── FileEditTool/       # 文件编辑
│   ├── GlobTool/           # 文件匹配
│   └── GrepTool/           # 代码搜索
│
├── Web 工具 (2 个)
│   ├── WebFetchTool/       # 网页抓取
│   └── WebSearchTool/      # 网络搜索
│
├── 代理工具 (2 个)
│   ├── AgentTool/          # 子代理调用
│   └── SkillTool/          # 技能调用
│
├── 任务工具 (7 个)
│   ├── TaskCreateTool/     # 任务创建
│   ├── TaskUpdateTool/     # 任务更新
│   ├── TaskListTool/       # 任务列表
│   └── ...
│
├── 集成工具 (4 个)
│   ├── MCPTool/            # MCP 工具调用
│   ├── LSPTool/            # LSP 集成
│   ├── ListMcpResourcesTool/
│   └── ReadMcpResourceTool/
│
├── 配置工具 (5 个)
│   ├── ConfigTool/         # 配置管理
│   ├── EnterPlanModeTool/  # 进入计划模式
│   └── ...
│
├── 通信工具 (3 个)
│   ├── AskUserQuestionTool/
│   ├── SendMessageTool/
│   └── BriefTool/
│
└── 条件工具 (可变)
    ├── SleepTool/          # feature('KAIROS')
    ├── CronCreateTool/     # feature('AGENT_TRIGGERS')
    └── PowerShellTool/     # isPowerShellToolEnabled()
```

**工具接口**:

```typescript
export type Tool<Input, Output, Progress> = {
  // 基础属性
  readonly name: string
  aliases?: string[]
  searchHint?: string
  
  // Schema
  readonly inputSchema: Input
  outputSchema?: z.ZodType<unknown>
  
  // 核心方法
  call(args, context, canUseTool, parentMessage, onProgress): Promise<ToolResult<Output>>
  
  // 权限与验证
  validateInput?(input, context): Promise<ValidationResult>
  checkPermissions(input, context): Promise<PermissionResult>
  
  // 特性检查
  isEnabled(): boolean
  isConcurrencySafe(input): boolean
  isReadOnly(input): boolean
  isDestructive?(input): boolean
  
  // UI 渲染
  renderToolUseMessage(input, options): React.ReactNode
  renderToolResultMessage?(content, options): React.ReactNode
  
  // ... 40+ 字段
}
```

---

### 2.6 服务层

**职责**: 提供外部系统集成和基础设施服务。

**核心服务**:

```
services/
├── mcp/ (25 文件)
│   ├── client.ts           # MCP 客户端
│   ├── config.ts           # 配置解析
│   ├── auth.ts             # OAuth 认证
│   └── ...
│
├── lsp/ (7 文件)
│   ├── manager.ts          # LSP 管理器
│   └── ...
│
├── api/ (22 文件)
│   ├── claude.ts           # Claude API
│   ├── client.ts           # API 客户端
│   └── ...
│
├── analytics/ (11 文件)
│   ├── index.ts            # 遥测入口
│   ├── growthbook.ts       # 特性标志
│   └── ...
│
├── compact/ (13 文件)
│   ├── autoCompact.ts      # 自动压缩
│   ├── microCompact.ts     # 微压缩
│   └── ...
│
├── plugins/ (5 文件)
│   ├── pluginLoader.ts     # 插件加载器
│   └── ...
│
└── voice/ (3 文件)
    ├── voice.ts            # 语音服务
    └── ...
```

---

### 2.7 扩展层

**职责**: 提供可扩展的技能和插件系统。

**Skills 系统**:

```
skills/
├── loadSkillsDir.ts (34KB)  # 技能加载核心
├── bundledSkills.ts (7KB)   # 内置技能
├── mcpSkillBuilders.ts      # MCP 技能构建
└── bundled/ (19 技能)       # 内置技能目录
```

**Plugins 系统**:

```
plugins/
├── discoverPlugins.ts       # 插件发现
├── installPlugin.ts         # 插件安装
├── loadPlugin.ts            # 插件加载
├── PluginManager.ts         # 插件管理器
└── types.ts                 # 插件类型
```

**Bridge (IDE 桥接)**:

```
bridge/
├── protocol/                # 桥接协议
├── handlers/                # 请求处理
├── clients/                 # 客户端实现
└── types.ts                 # 类型定义
```

---

### 2.8 基础设施层

**职责**: 提供通用工具函数和类型定义。

**Utils**:

```
utils/ (331 文件)
├── fs/                      # 文件系统工具
├── git/                     # Git 工具
├── http/                    # HTTP 工具
├── path/                    # 路径工具
├── string/                  # 字符串工具
├── array/                   # 数组工具
├── object/                  # 对象工具
├── date/                    # 日期工具
└── ... (更多工具函数)
```

**Types**:

```
types/ (10 文件)
├── index.ts                 # 类型导出
├── tool.ts                  # 工具类型
├── command.ts               # 命令类型
├── state.ts                 # 状态类型
└── ...
```

**Constants**:

```
constants/ (23 文件)
├── toolNames.ts             # 工具名称常量
├── commandNames.ts          # 命令名称常量
├── colors.ts                # 颜色常量
└── ...
```

---

## 3. 核心数据流

### 3.1 用户请求处理

```
用户输入
    │
    ↓
┌─────────────────────────────────┐
│ 1. main.tsx                     │
│    - parse CLI args             │
│    - init()                     │
│    - renderAndRun(<App />)      │
└───────────────┬─────────────────┘
                │
                ↓
┌─────────────────────────────────┐
│ 2. App.tsx → REPL.tsx           │
│    - 捕获用户输入                │
│    - 添加到消息队列              │
└───────────────┬─────────────────┘
                │
                ↓
┌─────────────────────────────────┐
│ 3. QueryEngine.submitMessage()  │
│    - wrappedCanUseTool          │
│    - fetchSystemPromptParts     │
│    - processUserInput           │
│    - recordTranscript           │
└───────────────┬─────────────────┘
                │
                ↓
┌─────────────────────────────────┐
│ 4. query() 生成器                │
│    - applyCompactions           │
│    - checkTokenLimit            │
│    - callModel() [流式 API 调用]  │
└───────────────┬─────────────────┘
                │
          ┌─────┴─────┐
          │           │
    [流式响应]   [检测到 tool_use]
          │           │
          │           ↓
          │   ┌─────────────────┐
          │   │ 5. runTools()   │
          │   │    - partition  │
          │   │    - canUseTool │
          │   │    - execute    │
          │   └────────┬────────┘
          │            │
          └────────────┴────────┐
                               │
                               ↓
                    ┌─────────────────────────┐
                    │ 6. 结果回灌到 query      │
                    │    - 构建 tool_result   │
                    │    - 继续循环或完成     │
                    └───────────┬─────────────┘
                                │
                                ↓
                    ┌─────────────────────────┐
                    │ 7. Ink 组件渲染输出      │
                    └─────────────────────────┘
```

---

### 3.2 状态变更流程

```
工具执行/用户操作
    │
    ↓
┌─────────────────────────────────┐
│ AppStateStore.setState()        │
│ (不可变更新：prev => ({...}))   │
└───────────────┬─────────────────┘
                │
                ↓
┌─────────────────────────────────┐
│ onChangeAppState()              │
│ - 持久化 Settings               │
│ - 发送遥测                      │
│ - 触发副作用 (MCP 重连等)        │
└───────────────┬─────────────────┘
                │
                ↓
┌─────────────────────────────────┐
│ Store 通知所有订阅者             │
│ listeners.forEach(fn => fn())   │
└───────────────┬─────────────────┘
                │
                ↓
┌─────────────────────────────────┐
│ Ink 组件重新渲染                 │
│ React 检测到状态变更             │
└─────────────────────────────────┘
```

---

### 3.3 工具权限检查流程

```
QueryEngine 检测到 tool_use
    │
    ↓
┌─────────────────────────────────┐
│ wrappedCanUseTool()             │
│ - 调用 canUseTool()             │
│ - 追踪 permission denials       │
└───────────────┬─────────────────┘
                │
                ↓
┌─────────────────────────────────┐
│ useCanUseTool.tsx (Hook)        │
│ - 检查 alwaysAllowRules → allow │
│ - 检查 alwaysDenyRules → deny   │
│ - 检查 alwaysAskRules → ask     │
│ - 检查权限模式                  │
└───────────────┬─────────────────┘
                │
          ┌─────┴─────┐
          │           │
     [允许]       [拒绝]
          │           │
          ↓           ↓
┌──────────────┐  ┌──────────────┐
│ 执行工具      │  │ 返回错误     │
│ BashTool     │  │              │
│   │          │  │              │
│   ├─→ 安全检查│  │              │
│   ├─→ 权限匹配│  │              │
│   └─→ 执行    │  │              │
└──────────────┘  └──────────────┘
```

---

## 4. 关键设计决策

### 4.1 为什么使用生成器模式？

**原因**: 自然表达异步流，支持嵌套生成器。

```typescript
// 流式处理
async function* query(): AsyncGenerator<StreamEvent, Terminal> {
  const stream = callClaudeAPI(...)
  for await (const event of stream) {
    yield event  // 边接收边推送
  }
  // 嵌套生成器
  if (hasToolUse) {
    yield* runTools(...)  // 展开工具执行流
    continue
  }
  return { type: 'complete' }
}
```

**优点**:
- ✅ 自然表达异步流
- ✅ 支持嵌套生成器 (`yield*`)
- ✅ 可随时暂停/恢复

---

### 4.2 为什么使用不可变状态？

**原因**: 可预测、易于调试、支持时间旅行。

```typescript
// ✅ 正确做法（不可变）
store.setState(prev => ({
  ...prev,
  tasks: { ...prev.tasks, [taskId]: newTask }
}))

// ❌ 错误做法（可变）
state.tasks[taskId] = newTask
```

**优点**:
- ✅ 可预测的状态变更
- ✅ 易于调试和追踪
- ✅ 支持时间旅行调试
- ✅ 避免副作用

---

### 4.3 为什么工具要分区执行？

**原因**: 读操作可并发，写操作需串行。

```typescript
// 分区逻辑
function partitionToolCalls(toolUseMessages) {
  return toolUseMessages.reduce((acc, toolUse) => {
    const isConcurrencySafe = tool.isConcurrencySafe(input)
    
    if (isConcurrencySafe && acc[acc.length - 1]?.isConcurrencySafe) {
      // 合并到并发批
      acc[acc.length - 1].blocks.push(toolUse)
    } else {
      // 新建串行批
      acc.push({ isConcurrencySafe, blocks: [toolUse] })
    }
    return acc
  }, [])
}
```

**优点**:
- ✅ 读操作并行执行，提高效率
- ✅ 写操作串行执行，保证一致性
- ✅ 自动判断，无需手动指定

---

### 4.4 为什么使用编译时特性标志？

**原因**: 零运行时开销，按需打包。

```typescript
// Bun 编译时 DCE
const coordinatorModeModule = feature('COORDINATOR_MODE')
  ? require('./coordinator/coordinatorMode.js')
  : null

// 编译后，未启用代码被完全移除
```

**优点**:
- ✅ 零运行时开销
- ✅ 类型安全
- ✅ 按需打包

---

## 📚 相关文档

| 文档 | 说明 |
|------|------|
| [01-快速开始.md](./01-快速开始.md) | 5 分钟上手 |
| [02-学习路线.md](./02-学习路线.md) | 20-40 小时学习路径 |
| [03-核心概念.md](./03-核心概念.md) | 基础术语详解 |
| [11-核心引擎.md](./11-核心引擎.md) | QueryEngine + query 循环 |
| [12-状态管理.md](./12-状态管理.md) | createStore + AppState |

---

_最后更新：2026-04-02_  
_预计阅读时间：45 分钟_
