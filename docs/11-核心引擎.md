# 核心引擎

> ⚙️ QueryEngine + query 循环详解 | 理解 Claude Code 的心脏

---

## 🎯 学习目标

完成本节后，你将理解：
- ✅ QueryEngine 的职责和结构
- ✅ query 循环的工作机制
- ✅ 流式处理原理
- ✅ 工具调用检测与执行
- ✅ 结果回灌机制

---

## 1. QueryEngine 类

### 1.1 职责

**QueryEngine** 是 Claude Code 的会话管理器：
- 管理会话生命周期
- 处理消息提交
- 调度工具执行
- 持久化会话状态

### 1.2 类结构

```typescript
// source/src/QueryEngine.ts

export class QueryEngine {
  // ═══════════════════════════════════════════════════════════════
  // 配置（不可变）
  // ═══════════════════════════════════════════════════════════════
  private config: QueryEngineConfig
  
  // ═══════════════════════════════════════════════════════════════
  // 会话状态（可变，跨轮次持久化）
  // ═══════════════════════════════════════════════════════════════
  private mutableMessages: Message[]           // 消息历史
  private abortController: AbortController     // 取消控制
  private permissionDenials: SDKPermissionDenial[]  // 权限拒绝追踪
  private totalUsage: NonNullableUsage         // Token 使用统计
  private readFileState: FileStateCache        // 文件读取缓存 (LRU)
  private discoveredSkillNames: Set<string>    // 技能发现追踪
  private loadedNestedMemoryPaths: Set<string> // 记忆路径缓存
  
  constructor(config: QueryEngineConfig) {
    this.config = config
    this.mutableMessages = config.initialMessages ?? []
    this.abortController = config.abortController ?? createAbortController()
    this.permissionDenials = []
    this.readFileState = config.readFileCache
    this.totalUsage = EMPTY_USAGE
  }
  
  // ═══════════════════════════════════════════════════════════════
  // 核心方法：提交消息
  // ═══════════════════════════════════════════════════════════════
  async *submitMessage(
    prompt: string | ContentBlockParam[],
    options?: { uuid?: string; isMeta?: boolean }
  ): AsyncGenerator<SDKMessage, void, unknown> {
    // 实现见下方流程图
  }
}
```

### 1.3 配置接口

```typescript
export type QueryEngineConfig = {
  cwd: string
  tools: Tools
  commands: Command[]
  mcpClients: MCPServerConnection[]
  agents: AgentDefinition[]
  canUseTool: CanUseToolFn
  getAppState: () => AppState
  setAppState: (f: (prev: AppState) => AppState) => void
  initialMessages?: Message[]
  readFileCache: FileStateCache
  customSystemPrompt?: string
  appendSystemPrompt?: string
  userSpecifiedModel?: string
  fallbackModel?: string
  thinkingConfig?: ThinkingConfig
  maxTurns?: number
  maxBudgetUsd?: number
  taskBudget?: { total: number }
  jsonSchema?: Record<string, unknown>
  verbose?: boolean
  replayUserMessages?: boolean
  setSDKStatus?: (status: SDKStatus) => void
  abortController?: AbortController
  orphanedPermission?: OrphanedPermission
  includePartialMessages?: boolean
}
```

---

## 2. submitMessage 流程

### 2.1 完整流程图

```
submitMessage(prompt)
    │
    ├─→ 1. 解构配置 & 清空技能追踪
    │     this.discoveredSkillNames.clear()
    │
    ├─→ 2. 包装 canUseTool (追踪权限拒绝)
    │     wrappedCanUseTool = async (...) => {
    │       result = await canUseTool(...)
    │       if (result.behavior !== 'allow') {
    │         this.permissionDenials.push({...})
    │       }
    │       return result
    │     }
    │
    ├─→ 3. 构建系统提示
    │     fetchSystemPromptParts() → systemPrompt
    │
    ├─→ 4. 处理用户输入
    │     processUserInput() → { messages, shouldQuery, allowedTools }
    │
    ├─→ 5. 持久化消息到 transcript
    │     recordTranscript(messages)
    │
    ├─→ 6. 加载 Skills/Plugins (仅缓存)
    │     getSlashCommandToolSkills()
    │     loadAllPluginsCacheOnly()
    │
    ├─→ 7. Yield 系统初始化消息
    │     buildSystemInitMessage({...})
    │
    └─→ 8. 进入查询循环
          for await (message of query({...})) {
            yield formatForSDK(message)
          }
```

### 2.2 关键代码

```typescript
async *submitMessage(prompt, options) {
  const {
    tools, commands, mcpClients, canUseTool,
    getAppState, setAppState, persistSession,
  } = this.config
  
  // 1. 清空技能发现追踪
  this.discoveredSkillNames.clear()
  
  // 2. 包装 canUseTool (追踪权限拒绝)
  const wrappedCanUseTool: CanUseToolFn = async (...) => {
    const result = await canUseTool(...)
    
    // 追踪拒绝（用于 SDK 报告）
    if (result.behavior !== 'allow') {
      this.permissionDenials.push({
        tool_name: sdkCompatToolName(tool.name),
        tool_use_id: toolUseID,
        tool_input: input,
      })
    }
    
    return result
  }
  
  // 3. 构建系统提示
  const { defaultSystemPrompt, userContext, systemContext } = 
    await fetchSystemPromptParts({
      tools,
      mainLoopModel: initialMainLoopModel,
      mcpClients,
      customSystemPrompt,
    })
  
  const systemPrompt = asSystemPrompt([
    customPrompt ?? defaultSystemPrompt,
    appendSystemPrompt,
  ])
  
  // 4. 处理用户输入
  const {
    messages: messagesFromUserInput,
    shouldQuery,
    allowedTools,
    resultText,
  } = await processUserInput({
    input: prompt,
    mode: 'prompt',
    context: processUserInputContext,
    messages: this.mutableMessages,
  })
  
  // 5. 持久化消息
  this.mutableMessages.push(...messagesFromUserInput)
  if (persistSession) {
    await recordTranscript(messages)
  }
  
  // 6. 加载 Skills/Plugins
  const [skills, { enabled: enabledPlugins }] = await Promise.all([
    getSlashCommandToolSkills(getCwd()),
    loadAllPluginsCacheOnly(),
  ])
  
  // 7. Yield 系统初始化消息
  yield buildSystemInitMessage({
    tools, mcpClients, model, permissionMode,
    commands, agents, skills, plugins, fastMode,
  })
  
  // 8. 进入查询循环
  for await (const message of query({
    messages,
    systemPrompt,
    userContext,
    systemContext,
    canUseTool: wrappedCanUseTool,
    toolUseContext: processUserInputContext,
    fallbackModel,
    querySource: 'sdk',
    maxTurns,
    taskBudget,
  })) {
    yield formatForSDK(message)
  }
}
```

---

## 3. query 循环详解

### 3.1 循环状态机

```typescript
type State = {
  messages: Message[]                          // 当前消息
  toolUseContext: ToolUseContext               // 工具执行上下文
  autoCompactTracking: AutoCompactTrackingState | undefined  // 压缩追踪
  maxOutputTokensRecoveryCount: number         // 恢复重试计数
  hasAttemptedReactiveCompact: boolean         // 是否尝试反应式压缩
  maxOutputTokensOverride: number | undefined  // 输出 token 覆盖
  pendingToolUseSummary: Promise<...> | undefined  // 待处理工具摘要
  stopHookActive: boolean | undefined          // Stop Hook 激活
  turnCount: number                            // 轮次计数
  transition: Continue | undefined             // 继续原因
}
```

### 3.2 主循环逻辑

```typescript
async function* queryLoop(params): AsyncGenerator<..., Terminal> {
  // ═══════════════════════════════════════════════════════════════
  // 1. 初始化状态
  // ═══════════════════════════════════════════════════════════════
  let state: State = {
    messages: params.messages,
    toolUseContext: params.toolUseContext,
    maxOutputTokensOverride: params.maxOutputTokensOverride,
    autoCompactTracking: undefined,
    stopHookActive: undefined,
    maxOutputTokensRecoveryCount: 0,
    hasAttemptedReactiveCompact: false,
    turnCount: 1,
    pendingToolUseSummary: undefined,
    transition: undefined,
  }
  
  const budgetTracker = feature('TOKEN_BUDGET') ? createBudgetTracker() : null
  let taskBudgetRemaining: number | undefined = undefined
  
  // ═══════════════════════════════════════════════════════════════
  // 2. 主循环
  // ═══════════════════════════════════════════════════════════════
  while (true) {
    // 2.1 解构状态
    const { messages, toolUseContext, ... } = state
    
    // 2.2 技能发现预取
    const pendingSkillPrefetch = skillPrefetch?.startSkillDiscoveryPrefetch(...)
    
    // 2.3 Yield 请求开始事件
    yield { type: 'stream_request_start' }
    
    // 2.4 应用 Snip 压缩 (HISTORY_SNIP 特性)
    if (feature('HISTORY_SNIP')) {
      const snipResult = snipModule!.snipCompactIfNeeded(messages)
      messagesForQuery = snipResult.messages
      if (snipResult.boundaryMessage) yield snipResult.boundaryMessage
    }
    
    // 2.5 应用 Microcompact
    const microcompactResult = await deps.microcompact(...)
    messagesForQuery = microcompactResult.messages
    
    // 2.6 应用 Context Collapse
    if (feature('CONTEXT_COLLAPSE')) {
      const collapseResult = await contextCollapse.applyCollapsesIfNeeded(...)
      messagesForQuery = collapseResult.messages
    }
    
    // 2.7 应用 Auto Compact
    const compactionResult = await deps.autocompact(...)
    if (compactionResult) {
      // 构建压缩后消息
      const postCompactMessages = buildPostCompactMessages(compactionResult)
      for (const message of postCompactMessages) yield message
      messagesForQuery = postCompactMessages
      continue  // 重新进入循环
    }
    
    // 2.8 检查 Token 阻塞限制
    const { isAtBlockingLimit } = calculateTokenWarningState(...)
    if (isAtBlockingLimit) {
      yield createAssistantAPIErrorMessage({
        content: PROMPT_TOO_LONG_ERROR_MESSAGE,
        error: 'invalid_request',
      })
      return { reason: 'blocking_limit' }
    }
    
    // 2.9 调用 Claude API (流式)
    for await (const message of deps.callModel({
      messages: prependUserContext(messagesForQuery, userContext),
      systemPrompt: fullSystemPrompt,
      thinkingConfig: toolUseContext.options.thinkingConfig,
      tools: toolUseContext.options.tools,
      signal: toolUseContext.abortController.signal,
      model: currentModel,
      ...
    })) {
      // 处理流式事件
      if (message.type === 'tool_use') {
        toolUseBlocks.push(message)
      }
      yield message
    }
    
    // 2.10 检查是否有工具需要执行
    if (toolUseBlocks.length > 0) {
      // 执行工具
      for await (const update of runTools(
        toolUseBlocks,
        assistantMessages,
        canUseTool,
        toolUseContext,
      )) {
        if (update.message) yield update.message
        toolUseContext = update.newContext
      }
      
      // 结果回灌
      state.messages.push(...toolResults)
      state.transition = { type: 'tool_use' }
      continue  // 继续循环
    }
    
    // 2.11 完成
    return { type: 'complete' }
  }
}
```

---

## 4. 流式处理

### 4.1 API 调用

```typescript
// source/src/query/deps.ts

async function* callModel(params): AsyncGenerator<StreamEvent> {
  const response = await fetch('/v1/messages', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      model: params.model,
      messages: params.messages,
      system: params.systemPrompt,
      tools: params.tools,
      max_tokens: params.maxOutputTokensOverride,
      stream: true,  // 流式响应
    }),
    signal: params.signal,
  })
  
  const stream = response.body.pipeThrough(new TextDecoderStream())
    .pipeThrough(new EventSourceParserStream())
  
  for await (const event of stream) {
    const data = JSON.parse(event.data)
    
    // 处理不同类型的事件
    switch (data.type) {
      case 'message_start':
        yield { type: 'message_start', message: data.message }
        break
      case 'content_block_start':
        yield { type: 'content_block_start', contentBlock: data.content_block }
        break
      case 'text_delta':
        yield { type: 'text_delta', text: data.delta.text }
        break
      case 'tool_use':
        yield { type: 'tool_use', toolUse: data.delta }
        break
      case 'message_delta':
        yield { type: 'message_delta', delta: data.delta }
        break
      case 'message_stop':
        yield { type: 'message_stop' }
        break
    }
  }
}
```

### 4.2 事件类型

```typescript
type StreamEvent =
  | { type: 'message_start'; message: Message }
  | { type: 'content_block_start'; contentBlock: ContentBlock }
  | { type: 'text_delta'; text: string }
  | { type: 'tool_use'; toolUse: ToolUseBlock }
  | { type: 'message_delta'; delta: MessageDelta }
  | { type: 'message_stop' }
```

---

## 5. 工具调用检测与执行

### 5.1 检测机制

```typescript
// 在流式处理中检测 tool_use
for await (const message of deps.callModel({...})) {
  if (message.type === 'tool_use') {
    toolUseBlocks.push(message)
  }
  yield message
}

// 检查是否有工具需要执行
if (toolUseBlocks.length > 0) {
  // 执行工具
  for await (const update of runTools(...)) {
    yield update.message
  }
}
```

### 5.2 执行编排

```typescript
// source/src/services/tools/toolOrchestration.ts

export async function* runTools(
  toolUseMessages: ToolUseBlock[],
  assistantMessages: AssistantMessage[],
  canUseTool: CanUseToolFn,
  toolUseContext: ToolUseContext,
): AsyncGenerator<MessageUpdate, void> {
  let currentContext = toolUseContext
  
  // 1. 分区：并发安全批 vs 串行批
  for (const { isConcurrencySafe, blocks } of partitionToolCalls(
    toolUseMessages,
    currentContext,
  )) {
    if (isConcurrencySafe) {
      // 2. 并发执行读操作
      for await (const update of runToolsConcurrently(...)) {
        yield update
      }
    } else {
      // 3. 串行执行非读操作
      for await (const update of runToolsSerially(...)) {
        yield update
      }
    }
  }
}
```

---

## 6. 结果回灌

### 6.1 构建 tool_result

```typescript
// 工具执行后构建结果
const toolResult: UserMessage = {
  type: 'user',
  uuid: randomUUID(),
  createdAt: Date.now(),
  message: {
    role: 'user',
    content: [
      {
        type: 'tool_result',
        tool_use_id: toolUse.id,
        content: result.data,
        is_error: result.isError,
      },
    ],
  },
  toolUseResult: result.data,
  sourceToolAssistantUUID: assistantMessage.uuid,
}

// 回灌到消息数组
state.messages.push(toolResult)
```

### 6.2 继续循环

```typescript
// 结果回灌后继续循环
state.messages.push(...toolResults)
state.transition = { type: 'tool_use' }
continue  // 重新进入循环，模型会看到工具结果
```

---

## 7. 错误恢复

### 7.1 max_output_tokens 恢复

```typescript
// 检测 max_output_tokens 错误
if (apiError === 'max_output_tokens') {
  if (state.maxOutputTokensRecoveryCount < MAX_OUTPUT_TOKENS_RECOVERY_LIMIT) {
    // 计算新的限制
    state.maxOutputTokensOverride = calculateNewLimit()
    state.maxOutputTokensRecoveryCount++
    continue  // 重试
  }
  // 超过重试次数，返回错误
  return { type: 'error', error: 'max_output_tokens_exceeded' }
}
```

### 7.2 自动压缩触发

```typescript
// 检查是否需要自动压缩
const tokenWarningState = calculateTokenWarningState(
  tokenCountWithEstimation(messagesForQuery),
  toolUseContext.options.mainLoopModel,
)

if (tokenWarningState.shouldCompact && isAutoCompactEnabled()) {
  const compactionResult = await deps.autocompact(...)
  if (compactionResult) {
    // 应用压缩
    messagesForQuery = buildPostCompactMessages(compactionResult)
    continue  // 重新进入循环
  }
}
```

---

## 8. Token 预算管理

### 8.1 预算追踪

```typescript
// 创建预算追踪器
const budgetTracker = feature('TOKEN_BUDGET') ? createBudgetTracker() : null

// 检查预算
if (budgetTracker) {
  const currentUsage = budgetTracker.getCurrentUsage()
  if (currentUsage >= config.maxBudgetUsd) {
    return { type: 'error', error: 'budget_exceeded' }
  }
}
```

### 8.2 task_budget

```typescript
// task_budget.remaining 追踪
if (params.taskBudget) {
  const preCompactContext = finalContextTokensFromLastResponse(messagesForQuery)
  taskBudgetRemaining = Math.max(
    0,
    (taskBudgetRemaining ?? params.taskBudget.total) - preCompactContext,
  )
}
```

---

## 9. 关键设计决策

### 9.1 为什么使用生成器？

**原因**: 自然表达异步流，支持嵌套生成器。

```typescript
// 流式处理
async function* query(): AsyncGenerator<StreamEvent, Terminal> {
  const stream = callClaudeAPI(...)
  for await (const event of stream) {
    yield event  // 边接收边推送
  }
  // 嵌套生成器
  if (hasToolUse) {
    yield* runTools(...)  // 展开工具执行流
    continue
  }
  return { type: 'complete' }
}
```

**优点**:
- ✅ 自然表达异步流
- ✅ 支持嵌套生成器 (`yield*`)
- ✅ 可随时暂停/恢复

---

### 9.2 为什么结果回灌？

**原因**: 让模型看到工具执行结果，继续推理。

```
模型调用 → 工具调用 → 执行工具 → 结果回灌 → 模型继续推理
                              ↑
                              └────── 循环
```

**优点**:
- ✅ 模型知道工具执行结果
- ✅ 可以继续推理或调用其他工具
- ✅ 支持多轮工具调用

---

### 9.3 为什么分区执行？

**原因**: 读操作可并发，写操作需串行。

```typescript
// 分区逻辑
function partitionToolCalls(toolUseMessages) {
  return toolUseMessages.reduce((acc, toolUse) => {
    const isConcurrencySafe = tool.isConcurrencySafe(input)
    
    if (isConcurrencySafe && acc[acc.length - 1]?.isConcurrencySafe) {
      // 合并到并发批
      acc[acc.length - 1].blocks.push(toolUse)
    } else {
      // 新建串行批
      acc.push({ isConcurrencySafe, blocks: [toolUse] })
    }
    return acc
  }, [])
}
```

**优点**:
- ✅ 读操作并行执行，提高效率
- ✅ 写操作串行执行，保证一致性
- ✅ 自动判断，无需手动指定

---

## 📚 相关文档

| 文档 | 说明 |
|------|------|
| [10-整体架构.md](./10-整体架构.md) | 6 层架构全景 |
| [12-状态管理.md](./12-状态管理.md) | createStore + AppState |
| [36-执行编排.md](./36-执行编排.md) | 并发/串行分区详解 |
| [35-权限系统.md](./35-权限系统.md) | 权限检查流程 |

---

_最后更新：2026-04-02_  
_预计阅读时间：40 分钟_
